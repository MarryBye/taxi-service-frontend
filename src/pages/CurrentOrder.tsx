import React from 'react';

import { DefaultLayout } from '@/components/layout/DefaultLayout';

export default function CurrentOrderPage(): React.ReactElement {
    return (
        <DefaultLayout>
        </DefaultLayout>
    );
}